"use strict";

//Updates the view, using values from the model, and functions from the controller
function updateView() {
    
    document.getElementById('app').innerHTML = /*html*/`
    
    This isnt actually used, as everything was just added to index directly...

    `; 
    //initCanvas();
    //createBoxes();
    

   
}